//
//  AddView.swift
//  groni
//

import SwiftUI

struct AddView: View {
    var body: some View {
        VStack {
            Text("Add")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    AddView()
}

