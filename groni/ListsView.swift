//
//  ListsView.swift

//  groni
//

import SwiftUI

struct ListsView: View {
    var body: some View {
        VStack {
            Text("Lists page")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ListsView()
}

